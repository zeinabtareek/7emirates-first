import 'package:flutter/cupertino.dart';

class CountryListPop extends StatefulWidget {
  const CountryListPop({Key? key}) : super(key: key);

  @override
  _CountryListPopState createState() => _CountryListPopState();
}

class _CountryListPopState extends State<CountryListPop> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
